import 'package:firebase_core/firebase_core.dart' show FirebaseOptions;

/// Stub until you run `flutterfire configure` (replaces this file).
///
/// After FlutterFire generates options, set [isConfigured] to true or remove
/// the stub and use the generated class as-is.
class DefaultFirebaseOptions {
  static const bool isConfigured = false;

  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'Firebase is not configured. Run: flutterfire configure',
    );
  }
}
